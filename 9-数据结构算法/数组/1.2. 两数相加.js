/**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.next = (next===undefined ? null : next)
 * }
 */
/**
 * @param {ListNode} l1
 * @param {ListNode} l2
 * @return {ListNode}
 */
var addTwoNumbers = function (l1, l2) {
  let n = l1.reverse();
  console.log("N", n, l1);

  let res = null;
  let arr1 = [];
  return res;
};

let arr1 = [1, 2, 3];
let arr2 = [4, 5, 6];
console.log("结果:", addTwoNumbers(arr1, arr2));